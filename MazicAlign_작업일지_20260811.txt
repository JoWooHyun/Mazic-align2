================================================================================
                MazicAlign 작업 일지 및 진행 현황
================================================================================
  작성일: 2026-08-11
  프로젝트: MazicAlign - Top-down 레진 프린터용 하이브리드 슬라이서
  이전 일지: MazicAlign_작업일지_20260805.txt
  ※ 진행 상태의 단일 진실은 docs/통합로드맵.md — 이 일지는 세션 기록 보조
================================================================================


[1] 날짜별 작업 내역
================================================================================


  2026-08-11 | 리드 중간점검(실물 확인) + 프루사 정독 + B-7 긴급 수정 (PR #40)
  --------------------------------------------------------------------------
    리드가 새 컴퓨터(무설치 환경)에서 ZIP 다운로드로 v2를 직접 실행해
    S-4b-1 실물 확인 수행. 발견 문제를 AI가 코드 대조로 분류·수정 착수.

    [완료] 리드 중간점검 — PR #39 확인 포인트 4개 중 3개 PASS
      ①기둥+화살촉 표시 ✅ ②F5 유지 ✅ ③프리뷰 단면 ✅ ④회전 후 생성은
      미확정(리드 관찰은 "생성 후 회전" 케이스라 별개 항목).
      - 발견 문제 8건 분류 문서 신설: docs/피드백_중간점검_20260811.md
        B-1 생성 후 모델 이동 시 서포트 동반 기울어짐(정책 결정 필요)
        B-2 일정 시간 후 재생성 의심(진단 필요) / B-3 서포트 1,300개 과다
        B-4 디버그 오버레이 잔류 / B-5 정상 판정 2건 / B-6 오버레이 글자
        드래그 긁힘 + 축 표기 Z-up 문제 / B-7·B-8 아래 참조.

    [완료] B-7 발견·확정: 래스터화 even-odd XOR로 "겹침 솔리드 = 마스크 구멍"
      - 리드가 ZIP 마스크에서 서포트·모델 겹침부가 검은색(미경화)임을 포착.
      - 원인 특정: slice-rasterize.ts가 전체 폴리곤을 한꺼번에 even-odd로
        칠해 서로 다른 솔리드의 겹침이 XOR 취소됨. 화살촉 0.2mm 침투가
        설계 전제라 S-4b 전체의 성립 조건 → 최우선 등재.

    [완료] 프루사 SLA 서포트 소스 전문 정독 (S-1 연구 보강)
      - 문서: docs/연구_프루사서포트_정독_20260811.md (AGPL — 개념만, 코드 금지)
      - DefaultSupportTree.cpp 등 핵심 7파일 전문 + 커버리지 정리.
      - 핵심 발견: ①기둥 공유(클러스터당 기둥 1개+다리 합류)가 기본 구조
        → B-3(1,300개)의 정공법 ②폴백 0단 = 근처 기둥 합류 ③경사 다리 =
        반경 단위 걸어나가기 루프(결정적) ④꺾임점마다 구(Junction) 삽입
        ⑤중복점 제거·H1/H2 이웃 규칙·내부 히트 재발사.
      - S-4b-2 스펙 반영 제안 6건을 문서 7절에 정리.

    [완료] B-7 수정 — dev-cycle (계획·검수 Fable / 코드 Opus), 검수 1차 PASS
      - 브랜치: fix/v2-raster-union-b7 (origin/integrate/v2-mainline 기반
        워크트리, 커밋 d3ab557) → PR #40 (대상 integrate/v2-mainline)
      [완료] slice-geometry.ts — 삼각형 바깥 법선에서 단면 선분 방향 부여
        (t=ŷ×n) + chainSegments 유방향 전환(감김 방향 보존). 232줄.
      [완료] slice-rasterize.ts — even-odd → nonzero 감김수 채움. 121줄.
        겹침 솔리드=채움 / 속 빈 모델 내강=보존. 공개 시그니처 불변,
        호출자(프리뷰·ZIP·CTB 워커·gcode)·검출·마진(잠금) 전부 무수정.
      [신규] scripts/verify-slice-rasterize.mjs — 헤드리스 검증 6케이스
        (겹침/중첩/내강/면적/회전/조립 서포트 발 도넛 회귀). 구 코드로
        되돌리면 6건 FAIL 확인 — 테스트가 실제 버그를 검출함.
      - 검수: 감김 부호 수학 유도 + 소비자 감김 무관 코드 확인. PASS.
      - 수치: tsc 13→13 / lint 34→34 / build 성공 / verify 전 통과.
      - 부수 효과: S-4b-1의 부품 간 의도적 겹침(발↔기둥, 화살촉)이
        동심원 도넛로 새던 문제도 함께 해소.

    [완료] B-8 등재: FDM G-code 경로(polygon-clipper.ts union pftEvenOdd)에
      동종 버그 잔존 — B-7 검수 중 발견. DLP 주력이라 후속 처리.

    갱신 문서: 피드백_중간점검_20260811.md(신설), 연구_프루사서포트_정독
      _20260811.md(신설), 이 일지(신설). docs 커밋 0d97800·9dc67d7·fa46ea9·
      57c1d53·390055c (feat/slicer-step1-2).

    [완료] B-7 리드 실물 확인 FAIL 1건 → 재작업 → 재검수 PASS → 리드 머지
      - 리드 확인: 재설계 서포트·모델 겹침 부위에 초승달 구멍 잔존 (구 trunk 정상).
      - 원인 확정: Babylon STL 로더의 기본 Y/Z 스왑(반사)으로 모델 감김이
        데이터상 뒤집힘(−1) ↔ 재설계 조립 서포트는 정상(+1) → 겹침만 상쇄.
        "기본 서포트는 안 그런다"는 리드 관찰이 결정적 단서. 조립 부피 +6.03 실측.
      - 수정(9498b46): extractWorldTriangles(전 슬라이스 경로 단일 관문)에서
        부호 부피 기반 감김 자동 정규화(normalizeTriangleWinding) + 혼합 감김
        회귀 (g) 3항목. 재검수에서 5개 경로 전수 추적으로 우회 없음 확정.
      - **PR #40 리드 머지 완료 (68bb5e7).** 워크트리·로컬 브랜치 정리.

    [완료] 퀵픽스 묶음 (B-2·B-4·B-6) — dev-cycle 검수 1차 PASS → PR #41
      - B-2 원인 확정 2건: ①ViewerV2Page 레거시 stl-local→world 되돌림
        effect(0c83dd2 대응)가 재설계 점을 로드 1.5초 후 오폭(= "일정 시간"의
        정체) ②useSupportMeshSync 재생성 키의 stl-local 이중 inv(world).
      - B-4 생성 성공 시 디버그 오버레이 자동 정리(디버그 버튼 경로는 유지).
      - B-6 ViewportOverlays·ViewControls select-none.
      - 수정 5파일, 기존 경로 보존(옛 데이터 되돌림·world 점 키·브릿지 무영향
        검수 확정). tsc 13→13 / lint 34→34 / build 성공 / 헤드리스 2종 무회귀.
      - 산출: **PR #41 (리드 확인·머지 대기)** — 확인 포인트: 생성 후 10초
        대기해도 안 움직임 / 오버레이 자동 소멸 / 드래그 글자 안 긁힘.


  2026-08-13 | B-1+B-11 검수 완결(PR #43) + 뷰어 조작 2건(PR #44·#45)
  --------------------------------------------------------------------------
    전 세션에서 서버 과부하(529)로 중단됐던 B-1 검수를 재개해 완결하고,
    리드가 CHITUBOX 실물과 대조해 제기한 뷰어 조작 문제 2건을 처리.
    세 건 모두 뷰어 표시·조작 레이어 — 출력물(STL/CTB/G-code) 무영향.

    [완료] B-1(변형 시 재설계 서포트 무효화) + B-11(stl-local parent 유지)
      - 중단된 워크트리·WIP 커밋(0ea9db8) 그대로 인계받아 검수 재개.
      - 검수 1차 FAIL 3건 → 수정 → 재검수 PASS → **PR #43 머지(020bb3f).**
        ①[상] 무효화 삭제가 fire-and-forget이라 "회전 → 즉시 Ctrl+Z" 시
          삭제 완료 전 복원이 돌아 재설계 서포트 전량 소실 + undo 스택 불일치.
          addSupports가 store.add라 ConstraintError로 tx abort, void undo()라
          조용히 깨짐. → invalidationDone promise로 직렬화 + add→put 전환.
        ②[중] 점마다 remove(전체 스캔+setState) 반복 → 1,300개 기준 O(N²)
          ≈170만 읽기. → 단일 tx deleteSupportsByIds + removeMany(refresh 1회).
        ③[하] coordSpace 미지정 재설계 점이 patch·삭제 대상에 동시 포함되어
          Promise.all 전체가 죽던 경로 차단.
      - **교훈**: 수용 기준(verify 4종·tsc·lint·build)이 1차에도 전부 통과했는데
        결함 3건이 나왔다. 검증이 순수 함수만 덮고 삭제·undo 부수효과가
        사각지대였던 탓 — B-7과 같은 패턴.
      [신규] scripts/verify-invalidate-undo-order.mjs — **수정 전 구현을
        대조군으로 함께 돌려** "스크립트가 실제로 버그를 잡는가"를 증명.
        이후 신규 검증 스크립트의 규약으로 확립(B-12·B-13에서 연속 적중).

    [완료] 리드 CHITUBOX 실물 대조 → 뷰어 조작 문제 2건 원인 특정
      - 리드 캡처 4장 대조: ②↔③에서 95° 회전 후에도 링 방향 동일(world 고정),
        ④에서 회전 후 POSITION 0 유지, ①에서 Z=위. 우리는 3항목 모두 달랐다.
      - 문서: docs/진단_뷰어축·회전_20260813.md (원인·규모·근거 파일:라인)
      - 리드 지시 "고치진말고 원인파악해바라" → 조사만 수행 후 승인받아 착수.
      - ⚠️ 해석 함정 기록: "밑에 평면을 X,Y평면으로"를 문자 그대로 구현하면
        슬라이서가 깨진다(Y=높이 전제 35~40파일). 의도는 "Z를 높이로 표기".

    [완료] B-12 기즈모 world 축 고정 + 회전해도 POSITION 불변 → PR #44 머지(f7c70e3)
      - **신규 결함이 아니라 회귀**: 구 v1 babylon.utils.ts:378·384의
        updateGizmoRotationToMatchAttachedMesh=false가 v2 이관에서 누락
        (Babylon 기본값 true=local). POSITION 변동은 B-9가 회전축만 bbox
        중심으로 옮기고 좌표 기준점은 origin에 남겨둬 origin이 피벗 둘레를
        공전한 결과 — 리드 실측이 pos'=Rd·(pos−p)+p와 소수 3자리까지 일치.
      - 방침(리드 승인): 표시만 bbox 기준 환산, 내부 tx/ty/tz 저장 의미 불변.
      - 검수 1차 FAIL 2건 → 수정 → PASS.
        ①[상] anchorOffset 캐시 탓에 **회전 슬라이더를 끄는 동안** POSITION이
          14.018mm 드리프트(리드 모델 ~19mm)하다 손 떼면 복귀 — 목표 정면 위반.
          → 캐시 제거, 렌더 시점 라이브 피벗으로 환산(표시=t+(pivot−t)=pivot 항등식).
        ②[상] 프록시 identity화가 축별 스케일에서 전단(shear) 유발(10mm 정점당
          2.712mm, 스케일 (1.5,1,1)→(1.275,1,1.275) 뭉개짐) → placePivotProxy에
          orientation 인자(회전=identity, 스케일=mesh).
        ③ScaleGizmo는 flag=false를 **거부하는 no-op setter**(Babylon 소스 확인)
          라 해당 줄 삭제 — 콘솔 경고만 남던 것.
      - ⚠️ 결함 ①이 1차 검증을 통과한 이유: 검증이 회전 후 오프셋을 재계산해
        불변을 확인했는데 실제 패널은 재계산하지 않았다 — 스크립트가 배선보다
        관대한 조건을 테스트. (e) "고정 오프셋=패널 실동작" 케이스 추가로 해소.

    [완료] B-13 축 Z-up 표기 → **PR #45 (리드 테스트 대기)**
      - 표시 레이어만 환산(7파일), 내부 Babylon Y-up 유지. 금지 영역 변경 0건.
      - 매핑 = (x, −z, y), det +1(순수 회전 = Babylon X축 +90°).
        단순 스왑 (x,z,y)는 det −1(반사)이라 회전 부호가 뒤집힌다.
      - ⚠️ **이번 세션 최대 수확**: 계획서에 쓴 회전 "성분 교환"
        (dispRY=−intRZ 등)이 **틀렸다.** 축 치환이 Babylon Euler 합성 순서
        (YXZ)와 교환되지 않아 복합 회전에서 어긋난다. 예) 내부(10,20,30) →
        올바름(18.199,−26.132,13.056) vs 성분교환(10,−30,20).
        올바른 해법은 quaternion 켤레 q_disp = qM·q_int·qM⁻¹.
        **가장 위험한 성질: 단축 회전에서는 우연히 정확히 일치한다** —
        단축으로만 테스트했다면 그대로 통과시켰을 것이다.
      [신규] scripts/verify-axis-display.mjs — 74건. 대조군 ①단순 스왑 det=−1
        실측·회전 부호 반전, ②성분 교환 단축 3/3 통과·복합 4/4 불일치를
        그 자체로 assert. B-12 회귀 케이스(4개 축 드리프트 <1e-6mm) 포함.
      - B-12는 내부 축 공간 연산이라 축 변환 **안쪽**에 배치(순서 바꾸면
        오프셋이 엉뚱한 축에 더해짐).

    [운영] Fable 세션 한도(3:50pm 리셋)로 B-12 재검수·B-13 검수는 메인 세션이
      직접 수행(verify 전종 실행 + 코드 대조 + 매핑 독립 검산). 전 세션의 529와
      별개 사유. 계획·검수=Fable / 코드=Opus 체제는 유지하되, 한도 도달 시
      메인 세션 대행을 허용하는 것으로 실무 운영.

    갱신 문서: 진단_뷰어축·회전_20260813.md(신설), 테스트_체크리스트_20260813.md
      (신설), 통합로드맵.md, 인수인계_다음작업.md(0장 전면 개편), 이 일지.
      docs 커밋 b490979·7ff35fe·e6a2067·8af1f63·98883e4·452e4fb.


  2026-08-14 | 리드 실물 테스트 피드백 처리 (B-14·B-15) + 주간 보고
  --------------------------------------------------------------------------
    리드가 #43·#44·#45를 실물 테스트 → 통과 5건 / 신규 결함 3건 발견.
    B-14·B-15는 원인 특정 후 수정 완료, B-16은 리드 지시로 보류.

    [완료] 리드 실물 테스트 통과분 5건
      1-5 마우스 회전 링 드래그 "원하는대로 됨" / 1-6 복합 회전 "이상 없는듯함"
      (가장 걱정하던 항목) / 2-6 콘솔 Babylon 경고 없음 / 3-1 회전 후 즉시
      Ctrl+Z "돌아오는것 같다" / 3-3 수십 개 삭제 "한번에 사라지네".

    [완료] B-14 수치입력칸 전면 수정 — PR #47 머지 (e242573)
      - 리드 보고: "수치입력칸 이상함 모든게 이상함 서포트쪽도 그렇고 다이상함"
        "Y 90도 입력하려고 9를 누르면 89.9999999" "전체 선택후 0 넣으면 이상해짐"
      - **리드 진단이 정확했다** — "엔터쳐야 하는데 바로 적용되는 것 같다".
        원인: 제어 입력에 onChange 가 타자 한 글자마다 즉시 반영 + min/max 즉시 클램프.
        8ae06aa(B-12 이전)와 한 글자도 다르지 않은 **기존 버그** — 우리 산물 아님.
      - 계획서 4개 패널 + 구현 중 발견한 5번째(SupportParamsPanel) = 총 5개 일괄 교체.
        공통 NumberInput(Enter/blur 커밋·커밋 시점 클램프·빈 값 원복·표시 반올림) +
        순수 함수 utils/number-input.ts. 슬라이더는 즉시 반영 유지.
      - ⚠️ **잠복 버그 발견**: endDrag 가 렌더 클로저의 local 을 끝값으로 써서,
        숫자칸처럼 onBegin→onChange→onEnd 가 한 핸들러에서 동기 호출되면 setLocal
        미반영 탓에 **커밋이 통째로 사라진다**(화면엔 보이는데 DB 저장·undo 모두 안 됨).
        슬라이더는 이벤트가 분리돼 우연히 무사했다. latestRef 도입으로 수정.
      - 잔여(별건): "Y만 만져도 X·Z 바뀜"의 두 원인 중 하나만 해소. Euler 다대일
        성질은 입력칸에서 못 고친다((0,90,0)≡(180,90,180)). 자세는 정확, 표기만 다름.

    [완료] B-15 수직축 회전 시 재설계 서포트 유지 — PR #46 (테스트 대기)
      - 리드 보고: "노란색원잡고 돌려도 (바닥과 수평인데) 서포트 사라지네?"
        바닥과 수평인 링 = 수직축 둘레 회전 = 제자리 회전이라 기둥은 여전히 수직.
      - 원인: transformKeepsRedesignValid 가 rx/ry/rz 세 축을 모두 무효로 침.
        **XZ 평행이동을 예외로 둔 것과 같은 논리인데 회전 쪽에서 빠진 정책 오류.**
      - 비주석 코드 변경은 ry 검사 한 줄 제거가 전부.
      - 기울어진 모델에서도 성립함을 Babylon 실측(rx=37,ry=-30,rz=52 기준 델타가
        world Y 정확히 고정, 로컬 up 의 Y성분 변화 0.000000000). FromEulerAngles 가
        Y를 가장 바깥으로 합성(world yaw)하기 때문.
      - 과잉수정 방지로 rx/rz/ty/sy/XZ이동 5종은 수정 전후 판정 동일함도 단언.
      - PR #47 머지 후 최신 메인라인 위로 리베이스 → #43·#44·#45·#47+#46 을
        한 브랜치에서 함께 테스트 가능(312cb90). 리베이스 후 verify 8종 재검증 통과.

    [보류] B-16 두 서포트 경로 공존 — 리드 "일단 미뤄"
      - 리드 관찰: "서포트 자동생성... 모델이 아무리 돌아도 그지점으로부터 바닥까지
        거리로 서포트가 유지되네? 수평이동 제외하고는 사라져야하는데"
        "여기 서포트랑 내가만든서포트랑 다른거 같은데 언제 통합시킬꺼야?"
      - 기존(Support 탭)=world 좌표라 모델이 돌아도 기둥이 제자리 / 재설계(Dental 탭)
        =stl-local. 무효화(B-1)를 재설계 쪽에만 걸어 기존 경로는 방치 상태.
        **리드 관찰이 정확하다.** 통합 시점은 S-4b-2~S-4d(재설계가 기존을 완전
        대체하면 폐기 — disc 폐기와 같은 방식). S-4b-2 진행 중 재검토.

    [완료] 주간 보고 문서 작성 — docs/주간보고_20260810-0814.md
      붙여넣기용 3줄 요약 + 상세본. 이번 주 PR 9건 머지(#39~#45,#47)·1건 대기(#46).

    갱신 문서: 주간보고_20260810-0814.md(신설), 테스트_체크리스트_20260813.md,
      진단_뷰어축·회전_20260813.md, 통합로드맵.md, 인수인계_다음작업.md, 이 일지.


[2] 현재 상태 (2026-08-14 종료 시점)
================================================================================

  - S-4b-1: PR #39 머지(857437c) + 후속 B-1/B-11(PR #43) 머지 완료.
  - B-7(마스크 겹침=구멍): 완료 — PR #40 머지(68bb5e7). 감김 정규화 포함.
  - 퀵픽스(B-2·B-4·B-6): PR #41 머지 / B-9(회전 피벗): PR #42 머지(8ae06aa).
  - B-1+B-11: **완료 — PR #43 머지(020bb3f).**
  - B-12(기즈모 world 축 + POSITION 불변): **완료 — PR #44 머지(f7c70e3).**
  - B-13(축 Z-up 표기): **완료 — PR #45 머지(fd39425).**
  - B-14(수치입력칸 5개 패널): **완료 — PR #47 머지(e242573).**
  - B-15(수직축 회전 시 서포트 유지): 검증 통과 — **PR #46 열림, 리드 테스트 대기.**
  - B-16(두 서포트 경로 통합): 🔶 리드 지시로 보류. S-4b-2에서 재검토.
  - 리드 테스트 체크리스트: docs/테스트_체크리스트_20260813.md
    (ZIP은 **fix/v2-vertical-rotate-keep-b15** 브랜치 — 리베이스로 #43·#44·#45·#47
     + #46 이 전부 들어있어 한 번에 확인 가능).
  - 주간 보고: docs/주간보고_20260810-0814.md (이번 주 PR 9건 머지 + 1건 대기).
  - 리드 답변 대기: #44 컨펌 안건 — 회전 후 POSITION Y가 CHITUBOX처럼 0이
    되지 않음(우리는 모델높이/2). A(현행 유지)/B(자동 바닥 안착 추가)/
    C(Y만 바닥 기준, 흔들림 재발) 중 **AI 권고 = A + B는 별건 검토**.
  - 중간점검 잔여: B-3(점 과다 — S-4b-2 기둥 공유로 해소 예정)·B-8(FDM
    gcode evenodd)·B-10(G-code 성능, 리드 보류).
  - 다음 대형 작업 = S-4b-2 (3단 폴백+충돌회피+기둥연결+기둥공유).
  - 체제: 계획·검수 = Fable / 코드 = Opus. 한도 도달 시 메인 세션이 검수 대행.
  - 열린 PR: **#45**(B-13, 테스트 대기) / #29(자동검수, 보류).
  - 워크트리: 스크래치패드 하위 b12·b13(push됨), b1(#43 머지 후 정리 대상).
    메인 폴더(feat/slicer-step1-2)는 문서 전용 유지.
  - 상세 진행 상태: docs/통합로드맵.md + docs/인수인계_다음작업.md.

[3] 앞으로의 계획
================================================================================

  ── 다음 세션 즉시 ──
  1. **리드 PR #46 테스트 결과 확인이 먼저.** 문제 있으면
     fix/v2-vertical-rotate-keep-b15 에 수정 커밋을 쌓는다(새 브랜치 만들지 말 것).
     핵심 항목: 바닥과 수평인 링으로 회전 시 서포트 유지 / 다른 두 링은 여전히 삭제 /
     (#47 겸사) 회전칸 90 입력 후 Enter·전체선택 후 0·0.5 입력·입력 후 Ctrl+Z·F5 유지.
  2. 문제 없으면 PR #46 머지 → S-4b-2 착수.
  3. #44 컨펌 안건(A/B/C — 회전 후 POSITION Y가 CHITUBOX처럼 0이 안 되는 건)
     답을 받으면 로드맵에 반영. AI 권고 = A 유지 + B는 별건 검토.

  ── 서포트 완성 (트랙 A) ──
  4. S-4b-2: 3단 폴백 + 충돌 회피 + 기둥 연결. 프루사 정독 반영 6건 포함
     (기둥 공유·근처 기둥 합류·걸어나가기 루프·Junction 구·중복점 제거·실패 통지).
     착수 전 docs/설계_서포트재설계_20260720.md 4장 +
     docs/연구_프루사서포트_정독_20260811.md 7절 필독.
  5. S-4c 래프트 → S-4d UI 4분절·프리셋·수동편집 → 실기 검증.

  ── 프루사 기본기능 (트랙 B — 리드 우선순위 확정 후) ──
  6. 1차: 자동 배치 → 수치 입력 패널 → Place on face.
     (축 Z-up 표기는 B-13으로 선행 처리됨)

  ── 기존 대기 항목 ──
  7. B-8(FDM gcode evenodd) / Step 3-4 main 교체 / PR #29 보류 유지 /
     margin-detect 분리(지현규 컨펌) / R-1 undoLift / 실기 검증 묶음(A7·A8) /
     마우스 조작 CHITUBOX식(리드: 나중에).


================================================================================
                              END OF DOCUMENT
================================================================================
