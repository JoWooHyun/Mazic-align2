# 종합 코드리뷰 — v2 메인라인 전체 감사

- **일자: 2026-07-09 (목)** / 대상: `integrate/v2-mainline` @ PR #21 (3일간 머지된 PR 20개의 누적 결과)
- 방식: 독립 감사 에이전트 2기 병렬 (A: 슬라이서/출력 계열, B: dental 파이프라인/뷰어·상태) — 개별 PR 검수가 못 보는 **PR 경계를 넘는 교차 일관성·상태 수명**에 집중
- 각 항목: [심각도] [확신도] 위치 — 내용

---

## 총평: "코어는 건강, 이음새에 구멍" — 잘해오고 있는가? **예, 단 조건부**

- ✅ **잠금 보존 완벽**: 마진 알고리즘 잠금 15항목+수식이 잠금 문서와 전부 일치, 이식 후 후속 수정 0건 (git 이력 검증)
- ✅ **단일화 목표 달성**: 예상 시간 공식·리프트/딜레이 폴백은 실제로 한 곳으로 통일됨. undo 단위 설계 건강 (일괄 생성 = Ctrl+Z 한 번)
- ⚠️ **상급 결함 3건**: 전부 "개별 PR은 맞았는데 합치니 틀린" 유형 — PR 단위 검수의 구조적 사각지대에서 발생
- 결론: 지금까지의 방식(PR별 dev-cycle 검수)은 유효하나, **Phase 완료 시점마다 이런 종합 감사를 정례화**해야 함 (이번이 그 첫 사례이자 근거)

## 🔴 심각도 상 (즉시 수정 대상)

| # | 발견 | 왜 심각한가 |
|---|---|---|
| A1 | **FDM 첫 레이어 누락 회귀** — `gcode/slice-adapter.ts:31,86`이 v1에서 이미 고쳐진 버그(startZ=minZ+lh)를 재도입. 주석이 v1을 반대로 인용 | 첫 층 단면·서포트 foot 유실 → 출력물 접착 실패 직결. 검수도 잘못된 v1 인용을 기준으로 통과시킴 |
| B1 | **STL 변형 후 stale 검출 결과** — gizmo 회전/이동 시 마진·아일랜드 결과(world 좌표)가 무효화되지 않음. 튜브는 시각적으로 따라가서 정상처럼 보임 | 회전 후 "자동 서포트" 클릭 시 **옛 좌표 기준으로 배치/가드** — dental의 핵심 보증(마진 비침범)이 조용히 깨짐 |
| B2 | **브러쉬-카메라 충돌** — 원본은 좌클릭을 카메라 회전에서 제외(`buttons=[1,2]`)했는데 v2 이식에서 누락 | 색칠 드래그와 화면 회전이 동시에 발생 — 브러쉬 UX 파손 (이식 누락 회귀) |

## 🟡 심각도 중 (단기 정리)

- **A2** 노광 폴백 상수(2.5/30/5/0)가 5개 파일에 리터럴 중복 — 현재는 일치, 한 곳만 바뀌면 조용히 갈라짐 → `types/printer.ts` DEFAULT_*로 승격
- **A3** export 실패 시 사용자 피드백 전무 (catch 없음, unhandled rejection) / **A4** 취소 API 미배선 — busy 중 탈출구가 새로고침뿐
- **A5** G-code 생성 메인스레드 동기 (1000레이어 = 수십 초 프리즈, busy 가드도 없어 이중 실행 가능)
- **A6** 죽은 동기 경로 2개(makeCtbV4, exportLayersAsPngZip) — 워커 경로와 로직 중복 보유, "한쪽만 고침" 사고의 온상 (A1이 정확히 이 유형)
- **A7** 마스크(Z플립) vs G-code(무플립) Y축 방향 불일치 가능성 — 지금은 잠복, **2노즐 하이브리드에서는 치명적** → 실기 검증 필수 등록
- **B3** 칠 추가/부분 삭제 시 마진·아일랜드 stale (B1과 같은 계열 — 무효화가 삭제 경로에만 배선됨)
- **B4** 다중 STL에서 마진/아일랜드가 단일 슬롯 — 마지막 검출 STL에만 유효한데 UI는 구분 없음
- **B5** disc 재빌드 실패 시 map.delete 누락 → disposed mesh가 export/슬라이스 경로에 잔류 가능
- **B6** "검출 영역 자동 서포트" 반복 클릭 시 같은 자리에 중복 생성 (dedup 없음)
- **B7** islandResultRef가 대형 중간산물(sliceCells 전체)을 세션 내내 보유 — 수백 MB 상주 가능 (실사용처는 islandFaces뿐)

## 🟢 심각도 하 (정리 목록)

A8 CTB 버튼 비활성 상태로 관련 PR만 축적(실사용 검증 0회) · A9 FDM 슬라이스 평면 perturbation 없음(v1엔 있음) · A10 단위 오표기 1건+no-op 산술 · A11 워커 progress 스로틀 없음 · B8 언마운트 시 island ref 리셋 누락(기지 사항 잔존) · B9 bridgeClipCache 미정리 · B10 margin-guard bodyR이 사용자 조절값에 연동(잠금 취지 재확인 — 지현규 컨펌 항목) · B11 잠금 문서 표에 PLATE_EXCL·SAMPLE_STEP 미등재

## 프로세스 점검 ("잘해오고 있나"의 방법론 측면)

**잘 작동한 것**: dev-cycle 검수가 3일간 실버그 12건+를 머지 전에 차단 (기즈모 충돌, 메모리 릭 2건, 유령 레코드, 허공 서포트, busy 고착 등). 잠금 규약·하위 호환·산출물 동일성 같은 "계약 검증" 문화가 정착.

**한계가 드러난 것**: ① PR 단위 검수는 PR 경계를 넘는 상태 수명(B1·B3·B4)과 교차 일관성(A2)을 구조적으로 못 봄 ② "원본과 동일" 검증에서 원본 자체가 잘못 인용되면 검수도 같이 통과(A1) — 원본 인용 시 현재 상태 재확인 필요.

**개선 조치**: Phase/마일스톤 완료 시 종합 감사 정례화 (이 문서가 1회차). CLAUDE.md에 "이식 시 원본 인용은 현재 커밋 기준으로 검증" 규칙 추가 예정.

## 처리 결과 (당일 마감 추기)

- **코드 처리분 13건 전부 당일 완결**: PR #22(상급 3) · #23(A2·A6) · #24(A3·A4·B5·B6) · #25(B4·B7·B8·B9)
- **마감 최종 검수** (4개 PR 누적 재검증): 13건 착지 전건 확인, 잠금·게이트 무결.
  유일한 실결함(지연 무효화 타이머 레이스 — PR 조합에서만 드러나는 유형) → **PR #26으로 즉시 수정**
- 잔여: 실기 검증 묶음(A5·A7·A8), 팀원 확인(B10 등), 소소 정리(A9~A11 + 취소 typed 판별, undo-검출 UX 문서화)

## 권고 수정 계획 (우선순위)

1. **[긴급 — 다음 작업]** 상급 3건 일괄 수정: A1(첫 레이어 공식 정정) + B1·B3(무효화 배선: transform·증분 칠 시 마진/아일랜드 리셋) + B2(카메라 버튼 매핑)
2. **[단기]** A6(죽은 경로 삭제) + A2(노광 상수 승격) — A1 유형 재발의 구조적 차단 / A3·A4(에러·취소 UX) / B4·B5·B6(다중 STL·map·dedup) / B7·B8·B9(메모리)
3. **[중기·실기]** A5(G-code 워커화), A7(Y축 실기 검증), A8(CTB 실기 검증) — 실제 프린터 테스트와 묶어서
4. **[팀원]** B10(bodyR — 지현규), A7·A8(실기 — 장비 있는 사람), 잠금 문서 표 갱신(B11)
