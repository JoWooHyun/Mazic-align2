# 주간 보고 — 2026-08-17(월) ~ 08-21(금)

지난주 계획(리드 작성) 3항목 기준 달성 현황. 이번 주 PR **7건 머지 + 1건 대기(#54)**.

## 붙여넣기용 요약

```
Mazic-CERA Slicer
 - 서포트 자동 배치 알고리즘 고도화 완료 — 기둥 공유(클러스터링 + 기둥당 다리 상한),
   경사 다리(0단 기둥 합류 → 수직 → 45° 걸어나가기 → 모델 앵커 3단 폴백),
   충돌 회피(링 광선 8개 + 자체 고속 삼각형 인덱스) 구현. PR 7건 머지
 - 프루사 오픈소스 분석 반영 6건 전원 실코드 적용 — 기둥 합류·걸어나가기 루프·
   중복점 제거(0.1mm)·내부 재발사·기둥당 다리 상한·실패 점 카운트 통지 (개념만, 클린룸)
 - 서포트 개수 과다: 원인 2건 특정·수정 — ①기둥 공유로 접점당 기둥 1개 구조 탈피
   ②과다의 주범 = 자기지지면(45°+ 벽) 오검출 → 검출각 실적용으로 수정
   (합성 모델 오버행 점 −87%, 실물 수치는 차주 초 확인 예정. PR #54 머지 대기)
 - 실물 테스트 2회 수행 — 결함 3건(표면 파묻힘·특정 각도 팅김·기둥 하중 편중) 발견,
   전건 당일 원인 특정·수정·머지
 - 잔여: 기둥 연결(좌굴 방지)·실패 통지 UI — 차주 착수
```

## 지난주 계획 대비 상세

### ① 서포트 자동 배치 알고리즘 고도화 (기둥 공유·경사 다리·충돌 회피) — ✅ 완료

S-4b-2를 4조각으로 나눠 이번 주에 3.5조각 완료:

| 조각 | 내용 | PR |
|---|---|---|
| 2a | 경사 막대 + 꺾임 구(Junction) 조립 프리미티브 (파단 방지) | #50 머지 |
| 2b | 점 전처리(0.1mm 중복 제거) + **기둥 공유 클러스터링** | #51 머지 |
| 2c | **충돌 회피**(링 광선) + **3단 폴백**(경사 다리) + 생성 경로 배선 | #52 머지 |
| 2c-f | 실물 결함 3건 수정 (아래 ④) | #53 머지 |
| 2d | 기둥 연결(좌굴 방지) + 실패 통지 UI | 차주 |

부수 성과: 충돌 검사를 순수 모듈화하면서 기존에 "실물로만 확인 가능"하던 영역이
자동 검증권에 편입 (검증 스크립트 3종 신설 — 누적 15종).

### ② 서포트 개수 과다 (1,300개 → 대폭 감소) — 🔶 수정 완료, 실물 수치 확인 대기

원인이 두 겹이었고 둘 다 수정:

1. **기둥 공유** (#51·#52·#53): 점마다 기둥을 세우지 않고 지면 직행 가능한 점들을
   묶어 기둥 1개 + 경사 다리 합류. 기둥당 다리 상한(기본 8)으로 하중 편중 방지.
2. **검출 과잉이 주범** (#54, 머지 대기): 리드의 상용 슬라이서 대조("실린더에 20개
   미만으로 출력 잘 됨")로 특정 — 검출각(30°)이 판정에 실적용되지 않아 45°+
   자기지지 벽까지 전부 오버행으로 잡던 것을 수정. **합성 모델 실측: 오버행 점
   1,596 → 205 (−87%), 45° 벽은 47 → 0.** 실물 풀아치(기준 1,490점) 수치는
   #54 머지 후 리드 확인 예정.

### ③ 프루사 분석 결과 실반영 — ✅ 완료 (6건 전원)

정독 문서(연구_프루사서포트_정독_20260811.md) 7절의 반영 목록 6건이 전부 실코드에 들어감:
폴백 0단(근처 기둥 합류) / 경사 다리 = 반경 단위 걸어나가기 루프 / 링 광선+내부
재발사+안전거리 / 중복점 제거 0.1mm / 기둥당 다리 상한(max_bridges_on_pillar) /
실패 점 카운트 통지. ⚠️ AGPL — 개념만 채택(클린룸), 코드 이식 없음.

### ④ 계획 외 — 실물 테스트 왕복 2회 (B-7 교훈의 실천)

- 1차(08-21): 결함 3건 발견 — 표면 파묻힘(충돌 허용치 과대) / 특정 각도 생성 팅김
  (레이캐스트 브루트포스 비용 폭발) / 기둥 3개에 다리 978개(상한 부재).
  **3건 전부 당일 원인 특정 → 수정 → 검수 → 머지(#53).**
- 기타: B-15c·B-17·B-18 머지(#46·#48·#49), #44 POSITION Y 안건 A안 확정,
  CHITUBOX 분석 문서(ChatGPT 산출) 검토 — 방향 전환 근거 없음·부분 채택 4건 판단,
  줌 확대 한계 신규 등재(B-19, 재현 조건 관찰 중).

## 차주 계획(안)

1. #54 머지 → 실물 점 수 확인 (1,490 → ? — 목표: 상용 슬라이서 수준 접근)
2. S-4b-2d: 기둥 연결(15mm↑ 이웃 1개/35mm↑ 2개, 지그재그) + 실패 통지 UI
3. 이후 S-4c(바닥판/래프트) 착수 검토
