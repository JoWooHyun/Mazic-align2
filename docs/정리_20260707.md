# MazicAlign 통합 작업 정리본

- **날짜: 2026-07-07 (화)**
- 작성: AI (Claude, 조우현 지시) / 참여: 조우현(리드), 유승제(리뷰), 지현규(코드 제공)

---

## 1. 오늘 하루에 일어난 일 (한 문단 요약)

아침에 "3인 팀 + AI 협업 체계를 잡아달라"로 시작해, 팀 문서 체계 구축 → 세 사람의 갈라진 코드베이스 발굴·검증 → 아키텍처 결정(ADR 3건) → 공식 저장소 통일 → **v1 슬라이서 자산의 v2 이식(Phase 1)을 PR 6개로 완료**까지 하루에 진행했다. 모든 구현은 AI dev-cycle(계획→Opus 구현→검수→재작업→PR)로 수행됐고, 사람(유승제)이 리뷰·승인하고 사람(조우현)이 머지했다.

## 2. 확정된 결정 (변경 시 docs/아키텍처결정_20260707.md 갱신)

| 결정 | 내용 |
|---|---|
| 메인라인 | **유승제 v2** (`integrate/v2-mainline`). 단 조우현 우세 4개 영역은 조우현 것 이식 (완료) |
| 데이터 계층 | **IndexedDB**. Express/SQLite 백엔드 동결 (삭제 금지·신규 의존 금지) |
| 서포트 분담 | 지현규=배치 판단(마진/아일랜드 검출) → 유승제=구조물 생성(브릿지/CSG) 파이프라인 |
| 공식 저장소 | **github.com/JoWooHyun/Mazic-align2** (조우현 admin). popcaron/MazicAlign은 구 미러 — push 금지 |
| 마진 알고리즘 | 잠금 — `docs/references/feedback_margin_algorithm_lock.md`. 변경 전 지현규 컨펌 필수 |

## 3. 머지된 PR (integrate/v2-mainline 기준, 시간순)

| PR | 내용 | 비고 |
|---|---|---|
| #1 | FDM G-code 파이프라인 이식 (+840줄) | 2노즐 하이브리드 초석. v2에는 G-code가 전무했음 |
| #2 | 백엔드 없이 STL 열기 (파일 선택+드래그앤드롭) | 조우현 실사용 테스트에서 발견한 문제 해결 |
| #3 | 바닥/전환 레이어 노광 보간 + 프로파일 노광 설정 | v2 CTB 노광 하드코딩 발견·해소 |
| #4 | 압출(E) 정합 — 레이어·푸터 G92 E0 | **실기 FDM 투입 최소 조건 충족.** v1 승계 결함 수정 |
| #5 | 배치 슬라이스/출력 Web Worker화 | UI 프리즈 해결. ⚠️ 오클릭으로 리뷰 전 머지 — **유승제 사후 검증 3건 대기** (PR #5 코멘트) |
| #6 | DLP 출력 시간 정밀화 (리프트/딜레이 포함) | Phase 1 마지막 항목 |

**AI 검수가 잡은 실전 버그 5건**: 압출 오해석(레이어 경계 대량 리트랙트), 드래그 오버레이 영구 잔류, 내보내기 취소 시 busy 영구 고착, 노광 설정 stale closure, 푸터 리트랙트 기준점 오류.

## 4. 다음 할 일 (우선순위순)

### 사람이 할 일

1. **[유승제] PR #5 사후 검증 3건** — 대형 모델(500+레이어) 내보내기 중 3D 뷰 프레임 / 워커화 전후 ZIP 바이너리 diff / 프리뷰 마스크 육안 대조. 문제 시 PR #5에 코멘트 (즉시 수정/revert 대응)
2. **[유승제] P0-2** — `integrate/v2-mainline` pull 후 v2 정상 구동 확인 + PR 6개 반영분 훑어보기
3. **[지현규] P0-3** — 공식 저장소에서 v2 구동 확인 + `docs/아키텍처결정_20260707.md`·`docs/통합로드맵.md` Phase 2(본인 담당) 읽기
4. **[조우현] branch protection 설정 권장** — Settings → Branches → `integrate/v2-mainline`에 "Require approvals: 1" (오늘 같은 오클릭 머지 방지)
5. **[조우현] 차장님 복귀 시 (다음 주, P0-5)** — 차장님을 Mazic-align2 collaborator 초대, popcaron 저장소 아카이브 협의

### AI가 할 일 (다음 세션, 위 확인 후)

6. **Step 1-5 (소형)** — 리프트 설정 4종의 CTB 내보내기 배선 + lightOffDelay 폴백 통일 (추정 1.0s vs CTB 0.0s)
7. **Phase 2 시작 (지현규 P0-3 확인 후)** — Step 2-1부터: 마진/아일랜드 순수 알고리즘 모듈 이식 → 마진 로직 추출(잠금 준수) → v2 UI 통합 → 검출→생성 파이프라인 연결 (상세: 통합로드맵)
8. **Step 3-2b** — v2 메인라인 ESLint 설정 도입 (현재 설정 파일 자체가 없어 lint 검증 불가)
9. Phase 3 — v1 정리, CLAUDE.md 전면 재작성, main 교체, GitHub Actions 자동 리뷰

## 5. 참고 문서 지도

- 결정 근거: `docs/아키텍처결정_20260707.md` / 실행 계획·진행 상태: `docs/통합로드맵.md` (체크박스가 단일 진실)
- 분석 원본: `docs/코드베이스_통합분석_20260707.md` / 팀 규칙: `CLAUDE.md`, `docs/WORKFLOW.md`
- 날짜별 상세: `MazicAlign_작업일지_20260629.txt` (2026-07-07 항목)
- 작업 방법: Claude Code에서 `/dev-cycle <Step 이름>` — 계획→구현→검수→PR 자동
