# 인수인계 — 새 세션 시작점 (2026-09-04 세션 종료 시점)

- 작성: 2026-09-04 (금) 세션 마감. **새 대화창이 가장 먼저 읽을 문서.**
- 정본 우선순위: `CLAUDE.md`(규칙) → `docs/통합로드맵.md`(진행 상태의 단일 진실) → 이 문서(직전 세션 맥락)

---

## 1. 지금 상태 (한눈)

- 기준 브랜치 `integrate/v2-mainline`, 최신 릴리스 **v0.13.2-remove-github**.
  이건 **최종 버전이 아니다** — 개발 진행 중의 마일스톤일 뿐(리드 명시).
- 품질 기준선: **tsc 12 / lint 34** (전부 기존 이슈 — 신규 0 유지가 규칙),
  헤드리스 검증 8종 상시 PASS (`cd frontend && npx tsx scripts/verify-*.mjs`).
- 미커밋 없음. 열린 PR = **#29 하나**(CI 자동검수 — 리드 결정으로 보류, 건드리지 말 것).
- **브랜치 대청소 완료(2026-09-04)**: 병합 완료된 origin 브랜치 72개 + 로컬 브랜치·
  잔여 worktree 정리. 남긴 것은 딱 5개 —
  `integrate/v2-mainline` / `main`(구 v1) / `feat/slicer-step1-2`(v1 + 미이관 연구문서) /
  `dev/ji-margin-island`(지현규) / `chore/github-auto-review`(열린 PR #29).
- **태그·릴리스는 전부 보존** — 리드가 요구한 "버전별 개별 다운로드" 그 자체라
  지우면 다운로드 링크가 깨진다. 정리 대상 아님.

## 2. 직전 세션(9/1~9/4)에 한 것

로드맵·작업일지에 전부 반영돼 있음. 요약만:

| 버전 | 내용 |
|---|---|
| v0.11.1 | 코드리뷰 13건(HIGH 3 수정) + 프루사 격차 분석 |
| v0.12.0 | P-1 Export 출력영역 확인창 + P-2 검출 파라미터 UI 9종 |
| v0.12.1 | B-29 재설계 2회차 에러(공유 머티리얼 dispose) + Scale 숫자입력 |
| v0.13.0 | **S-2 검출 워커화**(대형 모델 프리즈 해소, 진행률+취소) + Scale % |
| v0.13.1 | B-30 검출 오버레이 잔류(삭제·변형 시 정리) |
| v0.13.2 | GitHub 저장·불러오기 **전면 제거**(리드 지시, -487줄) |

리드 실물 확인: 프리즈 해소·진행률·취소·Scale %·산출물 동일 — 전부 OK.

## 3. 다음 작업

### 리드가 예고한 것 (새 세션 주제)
**"다른 기능들 손볼꺼야 — 프린터 프로파일 설정이라던지 등등"**

프린터 프로파일 코드 맵 (조사 완료, 바로 착수 가능):
- `types/printer.ts` — 프로파일 타입 + DEFAULT_*(노광 4종·리프트 4종 폴백의 단일 소스.
  **규칙 6: 폴백은 여기 하나만** — 화면 추정과 파일 기록이 같은 값이어야 함)
- `hooks/usePrinterProfileStore.ts` — 전역 스토어(`useCurrentProfile`)
- `components/PrinterProfileDialog.tsx` (490줄) — 설정 다이얼로그.
  수치 입력은 **B-14 규약**(Enter/blur 커밋형 `NumberInput`) 이미 적용돼 있음
- `components/PrinterProfileSelect.tsx` — 헤더의 기종 선택 드롭다운
- 소비처: SliceSidePanel(노광·시간 추정), ctb-encoder, buildVolumeMm→플레이트·볼륨경고(C-2)
- ⚠️ 프로파일 값 변경은 **CTB 기록값이 바뀔 수 있음** — 규칙 5(산출물 보존):
  의도적 변경이면 PR에 before/after 명시

### 로드맵상 다음 차례 (리드 지시 대기 없이 이어갈 수 있는 것)
- **S-4b-2d 기둥 연결(좌굴 방지) + 실패 점 화면 표시** — "다음 착수 대상"으로 등재.
  프루사 개념 대비 마지막 1/6. 주간보고에도 차주 항목으로 적힘
- 그다음 S-4c 바닥판(래프트)+합성

## 4. 이 세션에서 나온 리드 결정 (로드맵 미기재분 포함)

- **재설계 검증용 파란 점·파란 버튼**: "굳이 보여줄 이유 있나" 제기 →
  최종 **"냅두자"**. 제거는 S-4d(UI 정리) 때 후보. 지시 오면 즉시 제거 가능(30분)
- **크기 측정(mm) 입력**: "나중에 따로 조사해서 하자, 급한 거 아님" (격차 G-8)
- **Scale**: 슬라이더 금지, 숫자 입력만, % 표기 (완료)
- **GitHub 저장/불러오기**: 완전 제거 (완료)
- **B-28(줌아웃 후 줌인)**: 리드 "아직 발견 못함" — **완전 해소로 단정하지 말 것**, 재발 시 재조사

## 5. 새 세션이 알아야 할 함정 (반복 사고 이력)

1. **Babylon boundingBox는 회전 시 형상보다 부푼다** — B-21·B-24 두 번 당함.
   회전+경계상자 조합이 보이면 실제 정점 순회로
2. **공유 머티리얼 메시에 `dispose(_, true)` 금지** — B-29. 첫 메시가 공유 머티리얼을
   죽여 2회차부터 터짐
3. **world 좌표 오버레이는 삭제·변형 두 경로 모두에 정리** — B-4·B-30 두 번 당함
4. **라이브러리 머티리얼 덮어쓸 때 원래 레시피 먼저 읽기** — B-23(emissive 포화)
5. **deps 누락** — CLAUDE.md 규칙 7. `ctx`(useSceneRefs)는 매 렌더 새 객체라 deps 금지
6. **AGPL 클린룸** — PrusaSlicer·DragonFruit(Open-Resin-Alliance) 모두 AGPL.
   개념만, 코드 열람·복사 금지. "개발용이니 괜찮다"는 성립 안 함(공개 저장소 + 웹앱)

## 6. 세션 운영 관례 (이 세션에서 확립·유지된 것)

- 작업 흐름: 수정 → 검증(tsc/lint/build/검증 8종) → PR(본문에 "조우현 확인 포인트") →
  머지(리드 위임) → 로드맵 반영 → 태그+릴리스(리드가 zip 받아 실물 테스트)
- 릴리스 노트 = 리드의 실물 테스트 체크리스트를 겸함. 쉬운 한국어로
- 리드는 비개발자 — 원인 설명은 비유 포함해서, 확인 항목은 번호 목록으로
- 새 검증 스크립트는 **대조군 원칙**(수정 전 구현·변조 구현이 실제로 FAIL 나는지 증명)
