# 리팩토링 — LLM 친화 구조 분리 (2026-07-20)

- 작성: 2026-07-20 (AI, 리드 지시)
- 배경: AI 세션이 큰 파일을 읽을 때 토큰 소모가 과도함. 500줄 이상 갓파일 3개를 **단일 책임 원칙(SRP)** 에 따라
  작은 파일로 분리해, 이후 AI 작업이 "필요한 조각만 읽는" 구조로 전환.
- 원칙: **행동 불변(behavior-preserving)** — 로직·수치·문자열·순서를 바꾸지 않고 순수 이동+분할만 수행.
- 진행 방식: WORKFLOW의 AI 사이클 (플래너 계획 → coder 구현 → reviewer 독립 검수 → PASS 시 커밋).

## 1. 대상 선정 (그리고 제외한 것과 이유)

v2 메인라인(`integrate/v2-mainline`) 기준 500줄 초과 파일 중:

| 파일 | 줄 수 | 처리 |
|---|---|---|
| `features/v2/components/BabylonScene.tsx` | 3,656 | ✅ **분리 (Task C)** |
| `features/v2/pages/ViewerV2Page.tsx` | 2,358 | ✅ **분리 (Task B)** |
| `features/v2/utils/gcode/gcode-generator.ts` | 508 | ✅ **분리 (Task A)** |
| `features/v2/utils/dental/margin-detect.ts` | 977 | ❌ 제외 — **잠금 규약**. 구조 변경도 지현규 컨펌 필요. 컨펌 후 별도 작업으로. |
| `features/v2/utils/dental/island-detection.ts` | 639 | ❌ 제외 — 서포트 재설계(S-4)에서 검출 단계가 재정비될 예정. 지금 쪼개면 이중 작업. |
| `features/v2/utils/dental/dental-support.ts` | 480 | ❌ 제외 — 동일 사유 (S-4에서 새로 짬). |
| v1 파일들 (`src/pages/ViewerPage.tsx` 1,520 등) | — | ❌ 제외 — v1 동결·제거 예정 (Step 3-2/3-4). |

> **S-4 구현 지침 제안**: 새 서포트 코드는 처음부터 "파일당 500줄 이하, 기능별 파일 분리"로 작성한다.
> 그러면 서포트 계열은 리팩토링 없이 자연히 LLM 친화 구조가 된다.

## 2. Task A — gcode-generator.ts (508 → 469 + 유틸 2개)

### 구조도

```
utils/gcode/
├── gcode-generator.ts   469줄  GCodeGenerator 클래스 (외부 API 불변)
│     generateHeader / generateFooter / generateLayerPreamble
│     generateLayer ─ 단계별 private 메서드로 분할:
│       buildWallPaths → buildGapFill → buildInfillBoundaries
│       → buildInfillLines → emitPaths → emitInfillLines
│       (기존 내부 클로저 addSegment/printPaths/printInfillLines를 메서드로 승격,
│        클로저 가변 상태는 EmitContext 객체로 참조 전달)
├── gcode-geometry.ts     71줄  순수 기하: optimizePaths, calculatePathLength,
│                               rotatePolygons, rotateLines
└── infill-generator.ts   98줄  스캔라인 인필 (generateGlobalInfill)
```

- 소비자(`fdm-gcode.ts`) 무수정 — public API가 그대로이기 때문.
- **동작 불변 증명**: 고정 입력(3레이어 × 설정 4변형: solid/grid/zigzag+z-hop/lines+갭필·리트랙션 off)의
  G-code 출력 SHA-256이 전후 동일 (`8b6efb57...`, 49,317자).

## 3. Task B — ViewerV2Page.tsx (2,358 → 640 + 17개 파일)

### 구조도

```
pages/
├── ViewerV2Page.tsx                640줄  데이터 훅 배선 + 핵심 공유 상태
│                                         (selectedIds/editMode/gizmoMode/sceneHandleRef 등)
│                                         + effect 3개(좌표 마이그레이션·Esc 해제·삭제 감지)
│                                         + 훅 조립 + JSX 골격 + BabylonScene 배선
└── viewer/
    ├── hooks/
    │   ├── types.ts                 21줄  데이터 훅 반환타입 파생 (공유 의존성 타입)
    │   ├── useSupportEditing.ts    499줄  서포트/브릿지/disc 배치·이동·삭제·복제 + 편집 상태
    │   ├── useBridgeControlPoints.ts 257줄  브릿지 변곡점 편집 5핸들러
    │   ├── useTransformCommit.ts   346줄  gizmo 커밋 → 부착 서포트 전파 + undo
    │   ├── useDentalWorkflow.ts    255줄  색칠/마진/아일랜드/검출→서포트 (runWithBusy 헬퍼)
    │   ├── useSliceExport.ts       282줄  마스크ZIP/CTB/G-code/STL 내보내기 + 프리뷰 상태
    │   ├── useStlDropImport.ts     115줄  네이티브 파일 열기 + 드래그앤드롭
    │   ├── useViewerShortcuts.ts    88줄  뷰 프리셋 0~6·Z/B·M/R/S 단축키
    │   └── useClipboardActions.ts   83줄  전체선택/복사/잘라내기/붙여넣기/undo/redo
    ├── components/
    │   ├── ViewerHeader.tsx        102줄  상단 툴바 (프로파일·내보내기·저장/열기)
    │   ├── ViewportOverlays.tsx    129줄  우상단 컨트롤 스택
    │   ├── SupportEditToolbar.tsx   95줄  서포트 편집 툴바 (Bridge/Disc/직선복원/삭제)
    │   ├── ViewportInfoPanels.tsx   63줄  색 범례 + 축·플레이트 정보
    │   └── ViewerSidePanel.tsx     156줄  Transform/Support/Dental 탭 스위처
    └── utils/
        ├── support-transform.ts     40줄  브릿지 변곡점 비례 이동 (proportionalMoveCps)
        ├── profile-exposure.ts      39줄  프로파일 → manifest 노광 변환
        └── file-naming.ts           24줄  복제 파일명 " copy" 접미사
```

- 상태 소유권: 서포트 편집 상태는 `useSupportEditing`, dental 상태는 `useDentalWorkflow`,
  내보내기 상태는 `useSliceExport`가 소유. 페이지는 교차 공유 상태만 유지 — 중복 useState 없음.
- `supportsRef`(stale-closure 방지 미러)는 페이지가 생성해 훅에 주입.
- JSX는 DOM 구조·className을 그대로 이동 (마크업 무변경).

## 4. Task C — BabylonScene.tsx (3,656 → 114 + 24개 파일)

### 구조도

```
components/
├── BabylonScene.tsx                114줄  본체: props 구조분해 + useSceneRefs +
│                                         훅 호출(순서 고정, 아래 §5) + 핸들 조립 + <canvas>
│                                         + 타입 re-export (공개 인터페이스 불변)
└── babylon/
    ├── babylon-scene-types.ts      381줄  BabylonSceneProps / BabylonSceneHandle(25 메서드)
    │                                      / GizmoMode / IslandStats
    ├── scene-refs.ts               333줄  ref 45개 → SceneCtx 묶음 + props→ref 미러링
    ├── scene-actions.ts            135줄  refreshHighlight/attachDragBehavior/syncGizmo/getActiveStl
    ├── dental-actions.ts           400줄  마진·아일랜드 검출/시각화/무효화/지연 스케줄러
    ├── dental-floodfill.ts         137줄  마진 BFS floodfill (fillMarginFromFace)
    ├── bridge-clip.ts              113줄  브릿지 CSG subtract + 캐시 (clipBridgeWithManifold)
    ├── support-keys.ts              62줄  서포트 rebuild/클립 캐시 키 (순수 함수)
    ├── mesh-cache.ts                25줄  캐시 vertex data → Mesh 재구성
    ├── hooks/
    │   ├── useSceneBootstrap.ts    199줄  engine/scene/camera/조명/plate + 렌더루프 + cleanup
    │   │   ├── setup-gizmos.ts     172줄  gizmo 3종 + 드래그 핸들러 (bootstrap 내부에서 동기 호출)
    │   │   ├── setup-pointer-handlers.ts 249줄  더블탭 + 클릭픽 옵저버
    │   │   └── dispose-scene.ts     81줄  전체 dispose 시퀀스 (순서 불변)
    │   ├── useFileMeshSync.ts      148줄  files→mesh 동기화 + 오버행 색
    │   ├── useSupportMeshSync.ts   175줄  서포트 mesh diff 동기화
    │   ├── useSelectionSync.ts      32줄  highlight/gizmo 재적용
    │   ├── useSlicePreview.ts      108줄  Z 슬라이스 단면 미리보기
    │   ├── useBridgeVisualization.ts 240줄  브릿지 마커/변곡점 sphere
    │   ├── useEditModeSync.ts       53줄  편집모드 전환 (드래그·카메라 버튼)
    │   └── useDentalBrush.ts       454줄  dental-brush 페인팅 (포인터/휠/오버레이)
    └── handle/                            useImperativeHandle 그룹별 빌더
        ├── camera-handle.ts         70줄  setView/fit/fitSelection/viewPlate
        ├── transform-handle.ts     193줄  previewTransform/좌표 변환/표면 질의
        ├── support-gen-handle.ts   194줄  generateAutoSupports/autoSupportIslands
        ├── slice-export-handle.ts  131줄  exportStl/getSliceMask/getFdmSliceInput 등
        └── dental-handle.ts         79줄  findDentalMargin/detectDentalIslands 등
```

- 공개 인터페이스(파일 경로·export 이름·핸들 25메서드) 불변 → **소비자 파일 무수정**.
- 모든 조각은 `SceneCtx`(ref 묶음)를 인자로 받는다. `sceneRef`/`meshMapRef`/`isUnmountingRef`/
  `pendingInvalidationsRef`처럼 여러 조각이 관통하는 상태는 전부 ctx 소유.

## 5. 유지해야 하는 불변식 (다음 작업자·AI 필독)

BabylonScene 분리에서 **깨지면 안 되는 것들** — 코드 주석에도 명시돼 있음:

1. **훅 호출 순서 = 원본 effect 선언 순서** (bootstrap → fileMesh → supportMesh → selection →
   slicePreview → bridgeViz → editMode → **dentalBrush 마지막**). React는 unmount cleanup을 등록
   순서대로 실행하며, bootstrap cleanup이 먼저 `isUnmountingRef=true`를 세팅해야 brush cleanup이
   pending 무효화를 "즉시 실행"이 아닌 "타이머 정리"로 처리한다. **훅 순서를 바꾸면 안 된다.**
2. **dispose 순서** (`dispose-scene.ts`): isUnmounting 플래그 → … → `scene.dispose()` →
   `engine.dispose()` 맨 마지막. 순서 재배열 금지.
3. 이벤트 짝: canvas wheel은 `{capture:true}`로 등록·해제 모두, brush 포인터 옵저버는 같은 effect
   내 명시 remove, 더블탭·클릭픽 옵저버는 bootstrap에 남아 `scene.dispose()`가 정리.
4. gizmo 드래그 중 `setParent(mesh)` ↔ `setParent(null)` 짝, manifold wasm `delete()` 짝,
   dental-brush 이탈 시 카메라 버튼 `[0,1,2]` 원복.
5. `pendingInvalidationsRef`는 컴포넌트 레벨(SceneCtx) 소유 — 훅 로컬로 내리면 B3 레이스 회귀.

## 6. 검증 결과 (AI 사이클 검수 이력)

| Task | 구현 검증 | 독립 검수 |
|---|---|---|
| A (gcode) | G-code 출력 SHA-256 전후 일치(4설정 변형), build OK, lint 신규 0 | **PASS** — 원본 전문 대조, 순수 이동 확인 |
| B (ViewerV2Page) | tsc 13→13(신규 0), build OK, lint 신규 0, dev 스모크 OK | **PASS** — 대형 핸들러·JSX 전량 대조, 상태 단일 소스·stale-closure 안전 확인 |
| C (BabylonScene) | tsc 13→13(신규 0), build OK, lint 신규 0, dev 스모크 OK | **PASS** — 원본 3,656줄 전 구간 매핑(누락 0)·정규화 diff 대조, effect/dispose 순서·이벤트 짝 불변 확인 |

- tsc 기존 13건은 위치만 이동 (예: `undoLift` 기존 에러가 BabylonScene.tsx:1580 →
  `babylon/hooks/setup-gizmos.ts`로). **기존 버그도 "순수 이동" 원칙에 따라 고치지 않고 보존** —
  수정은 별도 작업으로.
- 검수에서 확인된 후속 권고 (이번 PR 범위 밖):
  1. **undoLift 잠복 버그(기존)**: Bridge 끝점 gizmo 드래그 커밋 경로에서 스코프 밖 함수를 참조 —
     원본에서도 런타임 에러가 났을 코드. `undoLift`를 공용 유틸로 추출해 import 하는 fix 커밋 권고.
  2. Task B 권고: `PendingBridge` 타입 export로 `unknown` 제거, `handlePick` useCallback 복원(보험).
- 린트 총계는 40 → 34로 감소. 감소분 5건은 전부 구 BabylonScene 소속으로 검수자가 부모 커밋과
  양쪽 lint 실행으로 원인 규명 완료 (규칙 분석 범위 변화·죽은 disable 지시문 해소 등 — 코드 누락·은폐 아님).
  새 구조에서 ctx 객체를 effect deps에 넣지 않기 위한 `eslint-disable` 8곳은 필수 억제(전부 실사용 확인).
- 자동 테스트가 없으므로 최종 확인은 WORKFLOW 수동 체크리스트 (PR의 "조우현 확인 포인트").

## 7. 앞으로의 규칙 (제안)

- **새 파일은 500줄 이하**를 기본 상한으로. 넘칠 것 같으면 처음부터 책임 단위로 나눈다.
- 갓파일에 기능을 "추가"하지 말 것 — 이번 분리로 생긴 디렉토리(`pages/viewer/`, `components/babylon/`)의
  해당 조각에 넣거나 새 조각을 만든다.
- margin-detect(977줄)는 지현규 컨펌을 받은 뒤 같은 방식(행동 불변 + 독립 검수)으로 분리한다.
